"""Student-developed extensions to :mod:`rice_dsm`.

Each contribution lives in its own explicitly imported subpackage.  This
namespace deliberately performs no automatic discovery or eager imports:
importing one student project must not execute or depend on every other
project.
"""
